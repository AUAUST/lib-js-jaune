/**
 * Used to privately store the red channel of a color.
 */
export const red = Symbol("red");

/**
 * Used to privately store the green channel of a color.
 */
export const green = Symbol("green");

/**
 * Used to privately store the blue channel of a color.
 */
export const blue = Symbol("blue");

/**
 * Used to privately store the alpha channel of a color.
 */
export const alpha = Symbol("alpha");

/**
 * Used to privately store the cached channels of a color.
 */
export const channels = Symbol("channels");

/**
 * Used to privately store the color conversions of a color.
 */
export const conversions = Symbol("conversions");
